<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.androidtutorials.sensor.nfcreader"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- In order to use NFC it is important to request the appropriate permission.
         If the application only works properly in the presense of NFC, you should
         also define the appropriate uses-feature. -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="true" />
    
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="org.androidtutorials.sensor.nfcreader.NfcTagReader"
            android:label="@string/title_activity_nfc"
            android:launchMode="singleInstance" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- The following intent filter will trigger for NDEF formatted
                 tags whose first NDEF Record is of of types TNF_WELL_KNOWN
                 and RTD_URI as well as have a "http" scheme. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="http" android:host="*" />
            </intent-filter>
        </activity>
    </application>

</manifest>