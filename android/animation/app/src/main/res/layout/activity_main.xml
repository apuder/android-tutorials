<?xml version="1.0" encoding="utf-8"?>



<!-- The layout places a soccer ball image in the top/left corner of the parent
     (the RelativeLayout). Note the use of a custom widget MyImageView that
     is referenced via its full qualified name. -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context="${relativePackage}.${activityClass}" >

    <org.androidtutorials.animation.MyImageView
        android:id="@+id/ball"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ball" />

</RelativeLayout>
